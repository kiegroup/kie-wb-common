These files are created in `kie-wb-common-dmn-webapp-kogito-marshaller` and have been copied here manually.

This webapp's `pom.xml` should be configured to extract and copy the files here automatically.