package org.kie.workbench.common.screens.library.client.settings.knowledgebases.item.knowledgesessions.listener;

import org.junit.Before;

import static org.mockito.Mockito.spy;

public class ListenerListItemPresenterTest {

    private ListenerListItemPresenter listenerListItemPresenter;

    @Before
    public void before() {
        listenerListItemPresenter = spy(null);
    }
}